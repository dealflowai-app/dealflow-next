/**
 * Re-export the useToast hook for convenience.
 * The canonical implementation lives in @/components/toast.
 */
export { useToast } from '@/components/toast'
export type { ToastAPI, ToastOptions, ToastType } from '@/components/toast'
